---
name: rust
description: Use when working on Rust code in this project, including implementation, refactors, warning cleanup, dependency work, and review of Rust-specific changes.
---

# Rust

## Instructions

- Never fix dead code warnings by ignoring dead code unless the user explicitly requests that approach. Find and address the actual problem.
- Never use `unsafe` unless the user explicitly requests it.
